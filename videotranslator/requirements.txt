# Core transcription, translation, audio, and Piper runtime.
--extra-index-url https://download.pytorch.org/whl/cu128
faster-whisper==1.2.1
truststore==0.10.4
piper-tts==1.6.0
# XTTS 0.27.5 imports APIs removed in Transformers 5.x. NLLB also works here.
transformers==4.57.6
sentencepiece==0.2.1
# Matched official CUDA 12.8 builds for the NVIDIA workstation. Runtime code
# still falls back to CPU when CUDA is inaccessible.
torch==2.11.0+cu128
torchaudio==2.11.0+cu128
scikit-learn==1.9.0
librosa==0.11.0
demucs==4.0.1

# Dedicated speaker diarization (pipeline step 3).
pyannote.audio==4.0.7

# Active-speaker and lip-motion analysis (pipeline step 7).
opencv-python-headless==4.13.0.92

# Expressive cross-lingual voice cloning. The XTTS-v2 model weights use the
# Coqui Public Model License and are enabled in this non-commercial project.
coqui-tts==0.27.5
